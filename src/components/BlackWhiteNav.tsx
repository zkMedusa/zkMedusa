import React from "react";

const BlackWhiteNav = () => {
  return (
    <div
      className="h-[50vh] w-full bg-cover bg-center flex items-center"
      style={{
        backgroundImage: `url('/pillars.gif')`,
        backgroundSize: "cover",
        backgroundPosition: "center",
      }}
    >
    </div>
  );
};

export default BlackWhiteNav;
